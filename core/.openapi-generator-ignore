# OpenAPI Generator Ignore
#
# Files listed here are NOT regenerated by openapi-generator. Each entry
# below is hand-owned by the factory and protected from clobbering.
#
# If you add an entry here, also justify it in the per-language
# OVERRIDES.md so future generator upgrades don't rediscover the same
# divergence the hard way.

# pom.xml is hand-owned: the apache-httpclient template's pom does not
# express the shaded-jar build, the full-distribution profile, or the
# Jersey-2 dep set this SDK actually ships with.
pom.xml

# Maven is the single supported build system. Gradle and SBT scaffolding
# is hand-deleted; this block keeps the generator from re-emitting it.
build.gradle
build.sbt
settings.gradle
gradle.properties
gradlew
gradlew.bat
gradle/

# Upstream openapi-generator scaffolding the factory does not use.
.travis.yml
git_push.sh

# CI workflow is hand-owned: matrix is pinned to the deployment-target
# JDK floor (Java 8) plus newer JDKs that trigger the strict-API
# enforcement profile in pom.xml. The upstream template doesn't know
# this matrix.
.github/workflows/maven.yml
